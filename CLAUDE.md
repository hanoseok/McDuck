# Claude Instructions

The canonical project guide is `AGENTS.md`.

Before changing this repository:

1. Open and read `AGENTS.md`.
2. Follow the guide in `AGENTS.md`.
3. Keep durable project guidance in `AGENTS.md`, not in this file.
4. If `AGENTS.md` is missing, recreate it as a valid Markdown guide with the same core sections: project overview, workflow, build/test commands, remote build/release/run flow, architecture, git rules, and documentation rules.

`CLAUDE.md` should stay as a short pointer so all agents share one guide source.
