# Claude Instructions

The canonical project guide is `AGENTS.html`.

Before changing this repository:

1. Open and read `AGENTS.html`.
2. Follow the guide in `AGENTS.html`.
3. Keep durable project guidance in `AGENTS.html`, not in this file.
4. If `AGENTS.html` is missing, recreate it as a valid HTML5 guide with the same core sections: project overview, workflow, build/test commands, architecture, git rules, and documentation rules.

`CLAUDE.md` should stay as a short pointer so all agents share one guide source.
